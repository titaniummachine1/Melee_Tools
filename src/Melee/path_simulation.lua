-- PathSimulation moved to Simulation/path_simulation.lua to keep all simulation code together.
-- This wrapper preserves old require paths for existing prototypes.
return require("Simulation.path_simulation")
